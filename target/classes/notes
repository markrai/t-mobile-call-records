DDL for DB:

-- records definition

CREATE TABLE records (
	datetime TEXT,
	destination TEXT,
	"number" TEXT,
	minutes INTEGER,
	direction TEXT,
	"type" TEXT
, name TEXT);


-- names definition

CREATE TABLE names (
	"number" TEXT,
	name TEXT
);